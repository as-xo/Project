[gd_scene load_steps=2 format=3 uid="uid://dlkxmkobyjtrn"]

[ext_resource type="Script" path="res://scripts/bubblesort_challenge.gd" id="1_j6kl4"]

[node name="Node2D" type="Node2D"]
script = ExtResource("1_j6kl4")

[node name="TextEdit" type="TextEdit" parent="."]
offset_right = 512.0
offset_bottom = 320.0
text = "func bubble_sort(arr):
    var n = arr.size()
    for i in range(n - 1):
        for j in range(n - i - 1):
            if arr[j] > arr[j + 1]:
                var temp = arr[j]
                arr[j] = arr[j + 1]
                arr[j + 1] = temp
    return arr"

[node name="Button" type="Button" parent="."]
offset_left = 432.0
offset_top = 280.0
offset_right = 512.0
offset_bottom = 320.0
text = "Run"
